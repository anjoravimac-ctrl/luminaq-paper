def normalize(data):\n    """Normalizes the input data to a range of [0, 1]."""\n    min_value = min(data)\n    max_value = max(data)\n    return [(x - min_value) / (max_value - min_value) for x in data]